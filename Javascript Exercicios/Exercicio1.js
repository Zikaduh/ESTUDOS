//	Vamos criar variaveis usando let, const e var, atribuir valores,
// identificar comportamentos e saídas quando tentarmos recuperar.


let a = 5; // são guardada no escopo local e pode ser alterada
var b = 10; // são guardada no escopo global e pode ser alterada
const c = 15; // funciona quase que nem um let, mas tem que ser inicializada e não pode ser reatribuida.

console.log(a + b + c)

